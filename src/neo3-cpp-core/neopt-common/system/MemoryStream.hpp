#pragma once

#include <assert.h>

// this is highly experimental

#include "types.h" // vbyte

namespace neopt {

class MemoryStream
{
public:
   vbyte ToArray()
   {
      assert(false);
      return vbyte(0);
   }
};

} // namespace neopt
